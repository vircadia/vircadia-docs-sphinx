# Environment Variables

You can alter Interface's functionality using these environment variables.

Uses the source files for the QML at runtime instead of the compiled files.

    HIFI_USE_SOURCE_TREE_RESOURCES=1

Enable OpenVR development mode.

    HIFI_DEBUG_OPENVR=1

